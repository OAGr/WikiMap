<div class="container">
	<td><%= link_to 'New Card', new_card_path( :parent_id => nil )%></td>

	<% @cards.each do |card| %>
			<div class="row" >
				<%= render_card_tree(card, 0) %>
			</div>
	<% end %>

	<div id="new_card_box">

	</div>
</div>